To run the DipFit program in the simulation mode, go through the following steps:

1) Open Terminal or Command Prompt.

2) Navigate to the root directory of the DipFit program:
   cd …/DipFit/
   
3) Run the program by the following command:
   python main.py examples/theory/config_hsFe_nitroxide.cfg

4) The results of the DipFit analysis will be outputted into the folder:
   DipFit/examples/theory/results/